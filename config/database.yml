# Postgres 9.2
#   gem install pg
#

# no production database because heroku takes care of that

# note that the databases have to be built before we can run this; will not be created by rails migrations

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

test:
  adapter: postgresql
  database: talkvite3_test
  password: Dr@g0n13
  pool: 5
  timeout: 5000

development:
  adapter: postgresql
  database: talkvite3_development
  password: Dr@g0n13
  pool: 5
  timeout: 5000

